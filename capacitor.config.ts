import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.syuleyman.safet.eindproject',
  appName: 'eindproject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
